package package1;
import package2.*;


public class B {
  private String privateMessage = "This is private";
  // something that is private is only accessible/visible by the class it is defined in.
  // So, only class B has access to privateMessage.
}
